{
  "name": "hocking-app",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "server",
    "shared",
    "client"
  ],
  "scripts": {
    "start": "npm run start --workspace=server",
    "dev": "npm run dev --workspace=server",
    "build": "npm run build --workspace=shared && npm run build --workspace=server",
    "install:all": "npm install && npm install --workspace=server && npm install --workspace=shared && npm install --workspace=client",
    "dev:client": "npm run dev --workspace=client",
    "dev:all": "concurrently \"npm run dev\" \"npm run dev:client\""
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
